spring:
  datasource:
    url: jdbc:mysql://localhost:3306/attendance_db
    username: root
    password: deeps
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: attendance-system-secret-key-2026

cors:
  allowed-origins: http://localhost:3000

server:
  port: 8080